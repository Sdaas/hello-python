from setuptools import setup, find_packages

setup(
    # The name of the distribution package. This is what users would typically
    # install via 'pip install hello-python'.
    name="hello-python",
    # The current version of the distribution package.
    version="0.1.0",
    packages=find_packages(),
)
